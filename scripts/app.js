'use strict';

function Project (title, descript, img, path) {
  this.title = title;
  this.descript = descript;
  this.img = img;
  this.path = path;
  //add timeDate prototype
}

function BlogEntry (title, content) {
  this.title = title;
  this.content = content;
  //add timeDate prototype
}
